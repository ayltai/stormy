export const handleError = console.error;

export { changeDarkMode, exit, getLocale, isDarkMode, openUrl, setAppAutoLaunch, updateMenu, } from './app';
export { convertSpeed, convertTemperature, } from './conversions';
export { getPartOfDay, } from './date';
export { capitalize, } from './string';
